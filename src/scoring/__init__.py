"""GlobeXAI Trade OS — Composite scoring & verdict business logic.

This package holds the scoring logic that previously lived inline as
n8n Code-node JavaScript (see docs/integration_architecture.md §6 —
business logic belongs in a real backend service, not in workflow JSON).
"""
