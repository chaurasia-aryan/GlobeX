# Sanya06C Issue Map

**Date:** 2026-08-24
**Source:** `gh` CLI is not installed on this machine, so issue data was pulled via the public GitHub REST API (`api.github.com/repos/chaurasia-aryan/GlobeX_Personal/issues`) instead. All 8 open issues are assigned to `Sanya06C`, which matches this session's git identity (`Sanya Chavan`). No issue in the tracker is assigned to Aryan or anyone else — Aryan's work (26 commits, most recently `6835d84`) has landed directly on `main` without issue tracking.

Every issue's status column below was re-verified against the current code on `main` @ `6835d84`, not assumed from the tracker text.

| # | Title | Scope / files | Depends on | Status now | Conflict risk |
|---|---|---|---|---|---|
| 1 | Zero RLS policies across 22 multi-tenant tables | `backend/database/supabase/migrations/20260824000000_row_level_security_and_schema_notes.sql` (new) | None | **FIXED (unverified live — no Docker)** — RLS enabled + policies on all 21 tables. Reviewed, not run against a live Postgres. | RED file area — landed as one new migration, existing migrations untouched |
| 2 | CORS: `allow_origins=["*"]` + `allow_credentials=True` | `main.py:127` | None | **FIXED** — now uses the existing `_allowed_origins` allowlist (was built but unused). Verified `main.py` still parses. | Low — single line change |
| 3 | Four hardcoded "live" status badges | `src/pages/EscrowPage.tsx`, `DocumentVerificationPage.tsx`, `BlockchainLedgerPage.tsx`, `ShipmentsPage.tsx` + their child components (`CryptoEscrowCard.tsx`, `ShipmentTracker.tsx`, `PublicTradeLedgerTable.tsx`, `DocumentVerificationStudio.tsx`, `escrowService.ts`) | None | **FIXED** — badges, subtitles, and embedded child-component claims (AIS LIVE, Sepolia, on-chain anchored, tx receipts) all corrected to honest labels. grep for the 4 original strings returns nothing. | Low — isolated files, verified clean via typecheck |
| 4 | `CreateListingPage` fabricates trust scores, never persists | `src/pages/CreateListingPage.tsx`, `src/services/api/aiService.ts`, `src/api/trades_api.py` | New `POST /api/v1/listings` (added) | **FIXED, with a known gap** — real backend persistence added (fail-closed, no fallback fake-success); trust/risk default to 0/unrated instead of fabricated 95/12/94/true. **Gap:** `WorkspaceContext`'s `appwriteService` auth is mock-only and has no real `organizations.id` UUID, so the real API call will currently 500 in the demo environment until auth-to-org wiring exists — that's now a visible, honest error instead of a silent fake success. | AMBER — touched `aiService.ts` (RED contract file) and `trades_api.py`; `WorkspaceContext.tsx` itself untouched |
| 5 | Business/compliance logic embedded in n8n workflow JSON | `backend/brain/n8n/globex_trade_automation.workflow.json`, `src/api/compliance_api.py` (new `POST /compliance/doc-verdict`, `POST /compliance/trade-synthesis`) | None | **FIXED, verified with tests** — same thresholds/weights (0.35/0.25/0.20/0.10/0.10 composite, 70 compliance floor, 75/45 recommendation bands) moved server-side; both n8n Code nodes now call the new endpoints via `this.helpers.httpRequest` instead of computing the decision inline. Diff on the workflow JSON is 2 lines (only the two `jsCode` fields), no other node/wiring touched. 11 new pytest cases pin the exact behavior; full suite 25/25 passing. | RED — landed as a single self-contained edit; not hand-merged, only the two node bodies changed |
| 6 | Branch divergence: `main` vs `dataset` disagree on frontend location | N/A here | — | **Not applicable on this checkout** — only `main` exists locally and on `origin` (`chaurasia-aryan/GlobeX_Personal`); no `dataset` branch. Likely already resolved by consolidation into this repo. | Verify with Aryan before closing — the tracker may be stale relative to this repo |
| 7 | Consolidate migration history (`escrow_accounts`/`blockchain_records` created→dropped→recreated) | Documented via `COMMENT ON TABLE` in the same new migration as #1 | Same RED area as #1 — done together | **FIXED (as documentation)** — did not rewrite the 3 already-applied migrations (shared history rule); added `COMMENT ON TABLE` pointing readers to the canonical (post-churn) definition. | RED — landed in the same single new migration file as #1 |
| 8 | Future work: real financial escrow | `blockchain/contracts/TradeLedger.sol` (currently no `payable`/deposit/release/refund) | Product decision on scope | Deferred by design — not in scope unless explicitly requested | N/A |

## Verification performed this session

- `main.py`: `python -c "import ast; ast.parse(...)"` — syntax OK.
- `trades_api.py`: same — syntax OK.
- Frontend: `npm install` (lockfile was already out of sync before this session — pre-existing drift, reverted `package-lock.json` afterward since it's out of my 4 issues' scope), then `npx tsc --noEmit`. Found one **pre-existing, unrelated** syntax error in `src/components/landing/SequencedTradeSimulator.tsx` (last touched in commit `132a1c7`, not this session) that blocks a full project-wide typecheck — reported, not fixed (not my file/issue). Re-ran typecheck excluding that file: all files touched this session compile clean. Along the way, fixed two real pre-existing bugs directly in my edit path: `CreateListingPage.tsx` referenced `user.id`/`user.city`, neither of which exist on `UserSession` (real field is `userId`, no `city` field at all) — silent `undefined` before, now corrected.
- Could not run `supabase start` (#1/#7) end-to-end — no Docker on this machine.

## Local environment gaps affecting this work

- **No Docker on this machine** → `supabase start` (local Postgres) cannot run here. Migration work (#1, #7) can be written and reviewed but not locally verified against a running instance without Docker. Flagging as a blocker for full verification, not for writing the SQL.
- **`gh` CLI not installed** → used the GitHub REST API as a substitute for read-only issue listing. Cannot update/comment/close issues without it. Human action needed if the team wants issue status updated from this machine.
- **No `.claude/` or `.agents/` skill directories** → this is a clean machine; none of the previously-installed skills (Caveman, Taste, etc.) carried over. Confirmed with the user before installing anything.
